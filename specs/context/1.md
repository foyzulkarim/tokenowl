---
task: "1"
title: "[Step 3] Layout shell — sidebar, header, 10 stub routes"
type: feat
branch: feat/1/layout-shell
source: GitHub issue #1
date: 2026-05-02
---

## Summary

Set up the full app shell — sidebar, header, and 10 stub routes — so every page is navigable before any real data is wired up. This is the foundation every subsequent step builds on.

## Context

Phase 1 · Step 3 of 17. Previous: Step 2 (Theme) ✅ · Next: Step 4 (Mock infrastructure)

## Packages to install

- `@tanstack/react-router` — typed routing + URL search params for filters
- `lucide-react` — icons for sidebar nav items
- `zustand` — sidebar collapse state, cross-page UI preferences

_(TanStack Query, MSW, shadcn/ui, Recharts added in later steps)_

## Build checklist

- [ ] Install the three packages above
- [ ] `src/routes/__root.tsx` — sidebar + header layout with `<Outlet />`
- [ ] Sidebar: 8 nav items (Dashboard, Sessions, Proposals, Patterns, Effectiveness, Chat, Runs, Settings) with Lucide icons and active-state highlight
- [ ] Header: app name (left), run status indicator placeholder — static "No runs" chip (right), global filter bar placeholder (center)
- [ ] Redirect `/` → `/dashboard`
- [ ] 10 stub route files — each renders a `<h1>` with the page name; no data

## Done when

- [ ] Sidebar navigates between all 10 pages
- [ ] URL changes on every sidebar click
- [ ] Active nav item is visually highlighted
- [ ] Layout holds correctly on every route
- [ ] All tokens used — no inline hex colors

## Commit

```
[step-3] layout shell — sidebar, header, 10 stub routes
```

## Key conventions (from CLAUDE.md + docs)

- Theme tokens only — `bg-surface`, `text-on-surface`, etc. No inline hex.
- Sidebar collapse state → Zustand store backed by `localStorage` under `tokenowl_` namespace.
- Filter bar state → URL search params via TanStack Router typed search.
- `__root.tsx` renders sidebar + header + `<Outlet />`.
