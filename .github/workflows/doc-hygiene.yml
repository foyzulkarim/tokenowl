name: Doc hygiene

on:
  pull_request:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Block branch-only artifacts from main
        run: bash .github/scripts/check-doc-hygiene.sh .
